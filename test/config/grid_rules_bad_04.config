# Invalid custom reward, invalid action
[ACTIONS]
L
R
U
D

[REWARDS]
DEFAULT = -1
1,4-D = 0    # Goal from the top
2,3-R = 0    # Goal from the left
3,4-P = 0    # Invalid action 'P'

[TRANSITIONS]
# Not used for now
# In future, allow to define custom transitions instead of using derived ones